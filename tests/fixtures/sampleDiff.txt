diff --git a/src/auth/tokenValidator.ts b/src/auth/tokenValidator.ts
index abc1234..def5678 100644
--- a/src/auth/tokenValidator.ts
+++ b/src/auth/tokenValidator.ts
@@ -1,10 +1,15 @@
 import { createHash } from "node:crypto";

+const TOKEN_EXPIRY_MS = 24 * 60 * 60 * 1000; // 24 hours
+
 export interface TokenPayload {
   userId: string;
   expiresAt: number;
+  scope: string[];
 }

 export function validateToken(token: string): TokenPayload | null {
+  if (!token || token.length === 0) {
+    return null;
+  }
   const hash = createHash("sha256").update(token).digest("hex");
   // TODO: look up token in database
   return null;
diff --git a/src/api/userEndpoints.ts b/src/api/userEndpoints.ts
index 111aaaa..222bbbb 100644
--- a/src/api/userEndpoints.ts
+++ b/src/api/userEndpoints.ts
@@ -5,8 +5,12 @@
 import type { TokenPayload } from "../auth/tokenValidator.ts";

 export function registerUserRoutes(app: App): void {
-  app.get("/users/:id", (req, res) => {
-    res.json({ id: req.params.id });
+  app.get("/users/:id", async (req, res) => {
+    const userId = req.params.id;
+    // BUG: missing authorization check before returning user data
+    const user = await db.users.findById(userId);
+    if (!user) return res.status(404).json({ error: "Not found" });
+    res.json(user);
   });
 }
