import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Matches the fleet docs pattern (protoMaker starter): VitePress DefaultTheme
// + a local brand stylesheet. Swap in @protolabsai/vitepress-theme here if/when
// the org standardizes on the shared theme package.
export default DefaultTheme
